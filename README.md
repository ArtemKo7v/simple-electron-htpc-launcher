# Simple Electron HTPC Media Launcher

How to run:
1. Install dependencies: npm install
2. Copy config/example.json to config/config.json and make changes
3. Start: npm start

For kiosk mode: npm start -- --kiosk
To enable debug tools: npm start -- --debugTools